spring:
  application:
    name: msvc-eureka

  cloud:
    config:
      enabled: false

server:
  port: 8761

#Eureka Configuration Properties
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
